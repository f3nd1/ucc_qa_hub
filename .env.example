# QMR Agent Office
#
# Phase 1 needs no environment variables. The OpenAI key is entered in the
# app's Settings and stored in the browser's localStorage only (browser-key
# pattern, matching the existing tool). Advice: use a spend-capped, scoped key.
#
# Supabase and ERPNext wiring arrive in a later phase; their config will live
# here at that point.
